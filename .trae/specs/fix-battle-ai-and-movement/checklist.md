\- [x] 严格复刻了原版 `FgtCmpMove` 的逻辑：位于 `CITY`（城池）地形上的单位在AI移动计算中直接返回，不发生移动。
- [x] 严格复刻了原版 `dFgtLandR` 的数据：`LandResistance` 表精确反映了各兵种真实地形移动力消耗。
- [x] 陆地兵种（如骑兵、步兵等）在拥有足够移动力时，能够按照正确的消耗值（如3）跨入水域地形。
- [x] 所有针对更新后移动逻辑的单元测试均能成功通过。

