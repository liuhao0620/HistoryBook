* [x] `useGameStore` 支持指令队列与回合报告队列。

* [x] 所有内政指令不再立即结算，而是推入队列，在回合结束时统一处理。

* [x] 玩家下达指令后，UI能弹出武将的“领命”反馈对话框。

* [x] 玩家点击“策略结束”后，底部依次展示各AI势力的“策略中...”提示，有一定延迟感。

* [x] 进入新回合初，通过阻塞式的弹窗，逐条向玩家展示上一回合的武将执行报告。

* [x] CSS主题变量已在 `index.css` 或 `App.css` 中定义（暗金、深棕风格）。

* [x] `GameButton`, `GamePanel` 和 `GameModal` 等可复用组件已创建并应用于 `GameScreen`。

* [x] 界面中的触控区域大小已适配移动端，提升操作体验。

* [x] `BattleScreen` 战斗界面的单位已由简单圆圈替换为角色头像。

* [x] `BattleScreen` 的顶部状态栏和侧边面板已应用新的主题样式。

